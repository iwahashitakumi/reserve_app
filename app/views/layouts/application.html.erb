<!DOCTYPE html>
<html>
  <head>
    <title>宿泊</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
   <header>
    <%= link_to "宿泊サービス", root_path, class: "text-white" %>
    <% if user_signed_in? %>
      <%= link_to "施設の新規登録", new_room_path %>
      <%= link_to "予約済み一覧", reservetions_index_path %>
      <%= link_to "アカウント設定", new_room_path %>
      <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "新規登録", new_user_registration_path do %>
      <%= link_to "ログイン", new_user_session_path do %>
    <% end %>
   </header>
    <%= yield %>
  </body>
</html>